{
  "name": "letterboxd-embed",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "postinstall": "patch-package",
    "patch-lb": "patch-package letterboxd-api"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "letterboxd-api": "^1.0.2",
    "patch-package": "^8.0.0"
  }
}
